# Copy to .env and fill in real values — never commit .env to git

# MariaDB connection
DB_HOST=127.0.0.1
DB_PORT=8787
DB_NAME=bako_iss
DB_USER=bako_user
DB_PASS=change_me

# API authentication (shared secret between ESP32 and server)
API_KEY=change_me

# VPS address (used by replay tools)
VPS_HOST=127.0.0.1
VPS_HTTP_PORT=8787
VPS_TCP_PORT=8766
