# Backend

FastAPI/Python backend server.

## Setup
1. `cd backend`
2. `python -m venv venv`
3. `source venv/bin/activate`
4. `pip install -r requirements.txt`
5. `uvicorn server:app --reload`

## API
See /docs for OpenAPI docs.

